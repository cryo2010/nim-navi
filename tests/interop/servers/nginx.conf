events {}
http {
  types { text/plain txt; application/octet-stream bin; }
  server {
    listen 8443 ssl;
    http2 on;
    server_name localhost;
    ssl_certificate     /certs/cert.pem;
    ssl_certificate_key /certs/key.pem;
    location / { root /htdocs; }
  }
}
