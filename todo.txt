# navi TODO

Status: [ ] planned  [~] in progress  [x] done (kept briefly for context)
Grouped by area. Items marked (gap-audit) came from the 2026-08 capability
audit vs Go net/http, axios, and httpx.

## Done on branch feat/http-client-gaps (HTTP-client gap closure)

Request / response identity
[x] Default User-Agent header (navi/<version>) when unset            (gap-audit)
[x] Default Accept: */* when unset                                   (gap-audit)
[x] res.text with Content-Type charset decoding (+ BOM sniff)        (gap-audit)
[x] Surface response trailers (h1 chunked + h2)                      (gap-audit)

Cookies
[x] Cookie prefix enforcement (__Host-/__Secure-)                    (gap-audit)

Redirects / proxy
[x] Strip Proxy-Authorization on cross-origin redirect               (gap-audit)
[x] SOCKS5 proxy + Proxy-Authorization for HTTP CONNECT              (gap-audit)

TLS
[x] In-memory CA bundle (TlsConfig.caBundle PEM string)              (gap-audit)
[x] Certificate pinning (SPKI SHA-256 pin)                           (gap-audit)
[x] Custom certificate-verification callback                        (gap-audit)

Transport / pooling
[x] Unix domain socket transport (sync/asyncdispatch/chronos; POSIX)
[x] Connection pool sizing: per-host cap (configurable), global idle cap,
    idle-timeout eviction (maxIdleConns/maxIdleConnsPerHost/idleConnTimeout)

## Backlog (not yet scheduled)

[ ] Active-connection cap + pool-acquire timeout (a blocking semaphore over
    OPEN connections, not just idle ones). Deferred from the pool-sizing work:
    the idle caps + timeout landed; a true in-flight cap needs a per-backend
    waiter queue. The concurrency-limit middleware already bounds in-flight
    requests as a practical stand-in.
[ ] OCSP stapling
[ ] CRL revocation checking
[ ] Custom DNS resolver / DoH / DNS caching
[ ] Cookie jar disk persistence (Netscape/JSON load-save)
[ ] AWS SigV4 request signing
[ ] Observability hooks (timing spans: DNS/connect/TLS/TTFB)
[ ] Request-body compression (Content-Encoding on upload)
[ ] Expect: 100-continue on request send
[ ] Unix domain sockets on the js backend (undici socketPath) and Windows AF_UNIX

## Done earlier (implemented; safe to drop on next cleanup)

[x] HTTP response caching (RFC 9111 subset: ETag/Last-Modified, max-age)
    -> src/navi/private/mw/httpcache.nim
[x] Client-side rate / concurrency limiting
    -> src/navi/private/mw/ratelimit.nim
